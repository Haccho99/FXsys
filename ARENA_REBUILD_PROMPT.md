# FX自動取引システム 基礎再構築タスク指示書（Arena.ai バトルモード用）

あなたは、最先端のクオンツ開発能力を持つAIモデルとして、以下の指示に厳密に従い、
FX自動取引システムの基礎を一から作り直す任務を遂行してください。
本指示書の目的は、既存システムの思想を継承しつつ、あらゆる面でより賢いアルゴリズムを構築し、
勝率と利益率をできるだけ向上させることです。

---

## 0. 参考リポジトリ（必読・最優先）

- 本タスクは「https://github.com/Haccho99/FXsys」を参考にすること。
- 作業開始前に、当該リポジトリ内のファイル（仕様書・設計思想・既存の取引判断の考え方・
  リスク管理の考え方・ディレクトリ構成・データ配置）をすべて読み込み、
  このFX自動取引システムがどのような思想で作られているかを正確に把握すること。
- 既存コードの単なる複写や、小手先のパラメータ調整・継ぎ足し修正は禁止する。
  思想を踏襲したうえで、基礎から再設計・再構築することを求める。

## 1. 目的と方向性

- 既存システムの設計思想（トレンド追随と勢いの確認、相場局面に応じた立ち回り、
  厳格なリスク管理、過学習を避けた最適化、実運用での堅牢性）を継承すること。
- そのうえで、エントリー判断・決済判断・相場環境認識・資金管理・リスク制御など、
  いろいろな面において既存より賢いアルゴリズムを自律的に考案し、組み込むこと。
- 最終目標は、バックテスト上で測定される勝率と利益率の向上である。
- どのような発想・工夫を採用するかはあなたの裁量に委ねる。
  ただし採用理由は成果物レポート内で説明できる状態にしておくこと。

## 2. バックテスト要件

- 価格データは、リポジトリ内の `data/global/market_data/` フォルダ配下に格納されているものを用いること。
  - 通貨ペアごとのフォルダ × 年単位のファイルという構成で、複数年分が格納されている。
  - 年単位ファイルは2023年〜2026年の4年分であり、2026年分は9月上旬までの途中のデータである点に留意すること。
  - 価格データの時間足・列構成・通貨ペア種別などの詳細は、実ファイルを直接確認して把握すること。
- バックテストは1年分だけでなく、フォルダ内に存在する複数年分のデータすべてを対象に、
  年ごとに区切って実施すること（単一年だけの評価は不可）。
- 元本100万円スタート換算で年間損益率を算出すること。
- 各年の評価では、少なくとも以下を測定・記録すること。
  - 年間損益率
  - 勝率
  - 最大ドローダウン
  - プロフィットファクター
  - その他、健全な評価に必要と判断した指標

## 3. 評価エージェントによる自己採点・再挑戦ループ

- アルゴリズムの構築が一通り完了するたびに、構築を行った本体とは別の
  「評価エージェント」を別人格として立て、採点を行わせること。
  - 評価エージェントの立場：コードを一切書かず、数値のみで判断する辛口のクオンツ・リスク管理担当。
  - 構築担当への情状酌量は一切なしとし、バックテスト結果の数値のみで厳格に判定すること。
- 各年のバックテスト結果に対する判定基準は以下とする。
  - 年間利益率+50%以上（元本100万円→150万円相当）かつ最大ドローダウンが一定水準以下：合格
  - 年間利益率+20%〜50%未満：改善の余地あり
  - 年間利益率+20%未満、または年単位で赤字：作り直しレベル
- 総合の合格基準は以下とする。
  - 対象年すべての平均で年間利益率+50%以上を達成すること。
  - かつ、深刻な過学習（特定の年だけ成績が良く、他の年は大きく崩れる状態）が見られないこと。
  - 年単位で赤字の年が一つでも残る状態では、合格としないこと。
- 合格基準に達しなかった場合の手順。
  1. 評価エージェントは、何が原因でスコアが低いのかを分析し、
     優先順位付きの課題リストとして提示すること。
  2. 構築担当は、その課題リストをもとにアルゴリズムを再構築し、
     再度バックテストと評価を行うこと。
- 再構築→再評価のサイクルは最大4回までとする（初回構築・評価に加えて、最大4回の再挑戦）。
  上限に到達しても合格基準に届かない場合は、その時点での最良の版を成果物とし、
  未達の理由・残課題・追加で試すべき方向性を最終レポートに明記すること。

## 4. 成果物と作業形式

- Arena.aiのコード生成機能（バトルモード）にはウェブ開発に特化したUIが用意され、
  最終的にはフロントエンド画面を用意するタスクになっているが、
  そのウェブUIには一切力を入れないこと（最低限の表示でよい）。
  あくまでもGitHubリポジトリ内のファイルを改変・追加する作業を主とすること。
- ワークスペース内には必ず「output」フォルダを設けること。
- 「output」フォルダの中には、元のGitプロジェクトのファイルと置き換えることで
  システムをアップデートできる状態のファイル群を用意すること。
  - ディレクトリ構成は元リポジトリの構成と対応させ、どのファイルがどこに対応するかが一目で分かる形にすること。
  - 新規追加ファイルと、既存ファイルの置換版が区別できるようにすること。
- 「output」フォルダ内には、以下のレポート類も同梱すること。
  - 年ごとのバックテスト結果レポート（§2の指標を含む）
  - 評価エージェントによる最終判定レポート（合否・スコアの根拠・各年の講評）
  - 置換手順書（どのファイルを、ローカルのどの場所に上書きすればよいかの一覧）
- 利用者は、生成されたすべてのファイルをダウンロードし、自分のPCのローカルフォルダに
  上書きする前提であるため、差分が明確で置換しやすい構成にすること。

## 5. 遵守事項

- 実データ（§2のフォルダ内の価格データ）に基づかない、架空の成績を報告しないこと。
- 未来のデータを参照するような、バックテストの公正性を損なう手法を用いないこと。
- 特定の年に都合の良い調整など、過学習の隠蔽にあたる行為をしないこと。
  頑健性と再現性を最優先とすること。
- 指示の解釈に迷う場合は、リスク管理上安全な側の解釈を採用し、
  その判断内容をレポートに記録すること。

## 6. 作業手順の要約

1. 参考リポジトリを読み込み、システムの思想を把握する。
2. 思想を継承しつつ、基礎からアルゴリズムを再設計・構築する。
3. 複数年分のデータを年ごとに区切ってバックテストする。
4. 評価エージェントが数値のみで採点する。
5. 不合格なら課題リストに基づき再構築する（最大4回まで）。
6. 「output」フォルダに置換可能な状態で成果物を整理する。
